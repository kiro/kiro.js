// Generated by CoffeeScript 1.4.0
(function() {
  var bootstrap, common, mixins,
    __slice = [].slice;

  bootstrap = window.BC.namespace("bootstrap");

  mixins = window.BC.namespace("bootstrap.mixins");

  common = window.BC.namespace("common");

  $.extend(this, common, bootstrap);

  bootstrap.tabs = function() {
    var active, content, id, tabList, tabs;
    tabs = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    id = nextId();
    active = once("active");
    tabList = ul({
      "class": "nav nav-tabs"
    }).foreach(tabs, function(tab, index) {
      return li(a({
        id: id + "_" + index,
        'data-toggle': 'tab'
      }, tab.name)).addClass(active());
    });
    active = once("active");
    content = div({
      "class": 'tab-content'
    }).foreach(tabs, function(tab, index) {
      return div({
        id: id + "_" + index,
        "class": 'tab-pane'
      }, tab.content).addClass(active());
    });
    return div({
      "class": 'tabbable'
    }, tabList, content);
  };

  bootstrap.tab = function() {
    var content, name;
    name = arguments[0], content = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
    return {
      name: name,
      content: content
    };
  };

}).call(this);

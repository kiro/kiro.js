// Generated by CoffeeScript 1.4.0
(function() {
  var body, bootstrap, docs, models,
    __slice = [].slice;

  docs = window.BC.namespace("docs");

  docs.examples = window.BC.namespace("docs.examples");

  bootstrap = window.BC.namespace("bootstrap");

  models = window.BC.namespace("models");

  $.extend(this, bootstrap, models, docs);

  body = function() {
    var items;
    items = 1 <= arguments.length ? __slice.call(arguments, 0) : [];
    return div(items);
  };

  docs.examples.chat = function() {
    return section(h1("Chat"), docs.code.email(), example("Chat client", "", function() {
      var message, messages, type;
      type = {
        ping: 'ping',
        text: 'text',
        join: 'join'
      };
      message = function(type, user, content) {
        return object({
          type: type,
          user: user,
          content: content
        });
      };
      return messages = collection();
    }));
  };

}).call(this);
